# DataNest Sprint 7 人工验收清单

> **验收方式**：页面上人工操作，按下面「填表数据」直接照填表单字段，逐项确认验收标准
> **范围**：Sprint 7 新增功能（数据资产目录 F1 + 任务模板库 F2 + 子 DAG 参数下发 F3 + Python 质量规则 F4 + §6.5 收尾补强）
> **账号**：admin（全功能，只验主链路；权限隔离用例用 API 快速验证，不切账号）
> **执行环境**：已全部部署到 docker（frontend / governance / engineering / worker / job / system / gateway / alert），环境 healthy
> **文件位置**：`docs/sprint7/测试用例清单.md`
> **填表约定**：`*` 表示必填项；标注「照填」的字段可直接复制填入；带「可选」的字段可不填。
>
> **✅ 自动化验证状态（2026-08-08）**：F1~F4 对应 E2E 已全部跑通（**57 passed**，见 handoff §6）。下方各模块为「手动复验」用途——自动化已证明功能可用，你按填表数据手动走一遍做最终确认即可。§6.5 收尾补强（质量任务调度语义）为 2026-08-09 新增，无自动化覆盖，需重点手动验证。

---

## 0. 环境准备与测试数据

### 0.1 环境速查

| 资源 | 地址/命令 | 账号 |
|------|-----------|------|
| 前端入口 | http://localhost:8080 | admin / admin123 |
| 接口文档聚合 | http://localhost:8080/swagger-ui.html | 匿名可读 |
| PostgreSQL 治理库 | `docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_governance` | datanest / datanest123 |
| PostgreSQL 工程库 | `docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_engineering` | datanest / datanest123 |
| PowerJob 控制台 | http://localhost:7700 | App 密码 powerjob123 |

### 0.2 启动确认

- [ ] `docker compose ps` 全部容器 Up / healthy
- [ ] 浏览器访问 `http://localhost:8080`，admin/admin123 可登录
- [ ] `http://localhost:8080/swagger-ui.html` 可打开（右上角可切 governance / engineering 服务）

### 0.3 获取 Token（API 用例用）

```bash
TOKEN=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"admin123"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")
echo $TOKEN
```

### 0.4 播种测试数据

> 测试数据统一 `e2e_s7` 前缀（固定 ID 900007 段），覆盖资产目录全维度：5 张元数据表、4 个分类节点、4 档质量评分、3 条质量规则 + 1 批次 3 明细、2 条血缘、1 个元数据数据源、3 个测试用户、1 个「另存为」候选同步任务。
> **执行方式**：复制整段到终端一次性执行，全部幂等（可重复执行）。

#### 播种 ① 测试用户 `datanest_system`

```bash
docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_system <<'EOSQL'
DELETE FROM sys_user_role WHERE user_id IN (SELECT id FROM sys_user WHERE username IN ('s7_govadmin','s7_engineer','s7_analyst'));
DELETE FROM sys_user WHERE username IN ('s7_govadmin','s7_engineer','s7_analyst');
INSERT INTO sys_user (id, username, password, email, status, created_at, updated_at) VALUES
(9000070000000001001, 's7_govadmin', 'Test123456', 's7.govadmin@test.io',  'ACTIVE', now(), now()),
(9000070000000001002, 's7_engineer', 'Test123456', 's7.engineer@test.io',  'ACTIVE', now(), now()),
(9000070000000001003, 's7_analyst',  'Test123456', 's7.analyst@test.io',   'ACTIVE', now(), now());
INSERT INTO sys_user_role (user_id, role) VALUES
(9000070000000001001, 'GOVERNANCE_ADMIN'),
(9000070000000001002, 'DATA_ENGINEER'),
(9000070000000001003, 'DATA_ANALYST');
EOSQL
```

#### 播种 ② 数据源 `datanest_engineering`

```bash
docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_engineering <<'EOSQL'
DELETE FROM datasource_connection WHERE id = 9000070000000000001;
INSERT INTO datasource_connection
(id, name, type, host, port, database_name, schema_name, username, encrypted_password,
 status, created_at, updated_at, auto_collect_on_save)
VALUES (9000070000000000001, 'e2e_s7_mysql_ds', 'MYSQL', 'middleware-test-mysql', 3306,
        'testdb', NULL, 'testuser', 'x', 'NORMAL', now(), now(), 0);
EOSQL
```

#### 播种 ③ 资产目录全套 `datanest_governance`

```bash
docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_governance <<'EOSQL'
-- 清理历史
DELETE FROM quality_check_detail WHERE table_id IN (9000070000000000011,9000070000000000012,9000070000000000013,9000070000000000014,9000070000000000015);
DELETE FROM quality_check_batch WHERE id = 9000070000000000051 OR job_name LIKE 'e2e_s7%';
DELETE FROM quality_rule WHERE id IN (9000070000000000041,9000070000000000042,9000070000000000043);
DELETE FROM quality_score WHERE id IN (9000070000000000031,9000070000000000032,9000070000000000033,9000070000000000034);
DELETE FROM lineage_record WHERE id IN (9000070000000000061,9000070000000000062);
DELETE FROM metadata_column WHERE table_id IN (9000070000000000011,9000070000000000012,9000070000000000013,9000070000000000014,9000070000000000015);
DELETE FROM metadata_table WHERE id IN (9000070000000000011,9000070000000000012,9000070000000000013,9000070000000000014,9000070000000000015);
DELETE FROM asset_classification WHERE name LIKE 'e2e_s7%';

-- 分类体系
INSERT INTO asset_classification (id, level, name, parent_id, sort, created_by, created_at) VALUES
(9000070000000000021, 'DOMAIN', 'e2e_s7_交易域', NULL, 1, 1, now()),
(9000070000000000022, 'TOPIC',  'e2e_s7_订单',  9000070000000000021, 1, 1, now()),
(9000070000000000023, 'TOPIC',  'e2e_s7_退款',  9000070000000000021, 2, 1, now()),
(9000070000000000024, 'DOMAIN', 'e2e_s7_用户域', NULL, 2, 1, now());

-- 元数据表
INSERT INTO metadata_table (id, datasource_id, database_name, schema_name, table_name, table_comment, source_status, source_type, data_domain, data_topic, owner_user_id, created_at, updated_at) VALUES
(9000070000000000011, 9000070000000000001, 'testdb', NULL, 'e2e_s7_trade_orders',  '电商交易订单主表',   'ONLINE', 'EXTERNAL',     'e2e_s7_交易域', 'e2e_s7_订单', 9000070000000001003, now(), now()),
(9000070000000000012, 9000070000000000001, 'testdb', NULL, 'e2e_s7_trade_refunds', '电商交易退款流水表', 'ONLINE', 'EXTERNAL',     'e2e_s7_交易域', 'e2e_s7_订单', NULL,               now(), now()),
(9000070000000000013, 9000070000000000001, 'testdb', NULL, 'e2e_s7_user_profile',  '用户画像宽表',       'ONLINE', 'EXTERNAL',     'e2e_s7_用户域', NULL,            NULL,               now(), now()),
(9000070000000000014, 9000070000000000001, 'testdb', NULL, 'e2e_s7_product_sku',   '商品维度表',         'ONLINE', 'EXTERNAL',     NULL,           NULL,            NULL,               now(), now()),
(9000070000000000015, -1,                  'datanest', NULL, 'e2e_s7_doris_mart',   'Doris 汇总表',       'ONLINE', 'BUILTIN_DORIS', NULL,           NULL,            NULL,               now(), now());

-- 元数据列
INSERT INTO metadata_column (id, table_id, column_name, data_type, column_comment, nullable, ordinal_position, source_type, source_status, created_at, updated_at) VALUES
(9000070000000000101, 9000070000000000011, 'id',       'bigint',  '主键',       false, 1, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000102, 9000070000000000011, 'trade_no', 'varchar', '交易流水号', true,  2, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000103, 9000070000000000011, 'amount',   'decimal', '订单金额',   true,  3, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000111, 9000070000000000012, 'id',       'bigint',  '主键',       false, 1, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000112, 9000070000000000012, 'refund_no','varchar', '退款单号',   true,  2, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000121, 9000070000000000013, 'id',       'bigint',  '主键',       false, 1, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000122, 9000070000000000013, 'user_id',  'bigint',  '用户ID',     true,  2, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000131, 9000070000000000014, 'id',       'bigint',  '主键',       false, 1, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000132, 9000070000000000014, 'sku_code', 'varchar', 'SKU编码',    true,  2, 'EXTERNAL', 'ONLINE', now(), now()),
(9000070000000000141, 9000070000000000015, 'id',       'bigint',  '主键',       false, 1, 'BUILTIN_DORIS', 'ONLINE', now(), now());

-- 质量评分（4 档）
INSERT INTO quality_score (id, table_id, table_name, datasource_id, score, health_level, pass_rules, warning_rules, severe_rules, last_checked_at, updated_at) VALUES
(9000070000000000031, 9000070000000000011, 'e2e_s7_trade_orders',  9000070000000000001, 95, 'EXCELLENT', 2, 1, 0, now(), now()),
(9000070000000000032, 9000070000000000012, 'e2e_s7_trade_refunds', 9000070000000000001, 85, 'GOOD',      2, 0, 0, now(), now()),
(9000070000000000033, 9000070000000000013, 'e2e_s7_user_profile',  9000070000000000001, 70, 'WARNING',   1, 1, 0, now(), now()),
(9000070000000000034, 9000070000000000014, 'e2e_s7_product_sku',   9000070000000000001, 20, 'BAD',       0, 0, 1, now(), now());

-- 质量规则（T1 三条启用）
INSERT INTO quality_rule (id, name, type, table_id, column_name, check_field, sql_expression, result_metric, warning_threshold, severe_threshold, weight, enabled, created_at, updated_at) VALUES
(9000070000000000041, 'e2e_s7_订单完整性', 'COMPLETENESS', 9000070000000000011, 'amount',   1, 'SELECT 0 AS null_rate',          'null_rate',          0.1, 0.5, 1, 1, now(), now()),
(9000070000000000042, 'e2e_s7_订单唯一性', 'UNIQUENESS',   9000070000000000011, 'trade_no', 1, 'SELECT 0 AS duplicate_count',    'duplicate_count',    0.0, 1.0, 1, 1, now(), now()),
(9000070000000000043, 'e2e_s7_金额范围',   'RANGE',        9000070000000000011, 'amount',   1, 'SELECT 0.5 AS out_of_range_rate','out_of_range_rate',  0.1, 0.9, 2, 1, now(), now());

-- 质量批次 + 明细（T1：2 PASS + 1 WARNING）
INSERT INTO quality_check_batch (id, job_id, job_name, trigger_type, status, started_at, ended_at, duration_ms, created_at, alert_sent) VALUES
(9000070000000000051, NULL, 'e2e_s7 质量页签批次', 'MANUAL', 'SUCCESS', now(), now(), 100, now(), 0);
INSERT INTO quality_check_detail (id, batch_id, rule_id, rule_name, rule_type, table_id, result_metric, result_value, success, executed_sql, created_at, result_level) VALUES
(9000070000000000052, 9000070000000000051, 9000070000000000041, 'e2e_s7_订单完整性', 'COMPLETENESS', 9000070000000000011, 'null_rate',          0,   1, 'SELECT 0 AS null_rate',          now(), 'PASS'),
(9000070000000000053, 9000070000000000051, 9000070000000000042, 'e2e_s7_订单唯一性', 'UNIQUENESS',   9000070000000000011, 'duplicate_count',    0,   1, 'SELECT 0 AS duplicate_count',    now(), 'PASS'),
(9000070000000000054, 9000070000000000051, 9000070000000000043, 'e2e_s7_金额范围',   'RANGE',        9000070000000000011, 'out_of_range_rate',  0.5, 1, 'SELECT 0.5 AS out_of_range_rate', now(), 'WARNING');

-- 血缘（user_profile → trade_orders → trade_refunds）
INSERT INTO lineage_record (id, source_table, target_table, source_column, target_column, dag_id, dag_name, node_id, node_name, execution_id, lineage_type, created_at) VALUES
(9000070000000000061, 'testdb.e2e_s7_user_profile',  'testdb.e2e_s7_trade_orders',  NULL, NULL, 1, 'e2e_s7_dag', 'n1', 'SQL节点', 1, 'SQL', NOW()),
(9000070000000000062, 'testdb.e2e_s7_trade_orders',  'testdb.e2e_s7_trade_refunds', NULL, NULL, 1, 'e2e_s7_dag', 'n2', 'SQL节点', 1, 'SQL', NOW());
EOSQL
```

#### 播种 ④ 任务模板「另存为」候选 `datanest_engineering`

```bash
docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_engineering <<'EOSQL'
DELETE FROM sync_job WHERE id = 9000070000000000071;
INSERT INTO sync_job (id, name, source_datasource_id, source_database, source_tables, sync_mode, trigger_type, target_database, target_table, created_by, created_at)
VALUES (9000070000000000071, 'e2e_s7_tpl_source_sync', 9000070000000000001, 'testdb', '["e2e_s7_src_orders"]', 'FULL', 'MANUAL', 'dwd', 'e2e_s7_tgt_orders', 1, now());
EOSQL
```

### 0.5 播种验证

- [ ] 登录 admin → 访问 `http://localhost:8080/asset-catalog`：左侧树含 `e2e_s7_交易域(2)` / `e2e_s7_用户域(1)`，右侧表格有数据行
- [ ] 访问 `http://localhost:8080/engineering/task-templates`：列表含 3 条内置模板

---

## 用例汇总

| 功能块 | 用例数 | 对应 PRD / 验收项 |
|--------|--------|-------------------|
| F1 资产目录 | 30 | DC-01 ~ DC-05、AC-10 权限 |
| F2 任务模板库 | 18 | DD-09 |
| F3 子 DAG 参数下发 | 12 | NG5 |
| F4 Python 质量规则 + CUSTOM_SQL | 12 | DG-10 |
| §6.5 收尾补强 | 6 | 质量任务调度语义回归 |
| **总计** | **78** | — |

---

## 一、F1 数据资产目录

> 入口：侧边栏「数据资产」（`/asset-catalog`）。admin 具备分类维护写权限（对齐治理员）。

### 1.1 首页加载与分类树浏览

- **操作步骤**：
    1. admin 登录 → 侧边栏「数据资产」
- **验收标准**：
    - [ ] 标题「数据资产」；左侧分类树含 `e2e_s7_交易域(2)` / `e2e_s7_用户域(1)` / `全部资产` / `未分类` 计数徽章
    - [ ] 右侧表格展示数据行；底部分页器「共 N 条」
    - [ ] 树节点计数：`e2e_s7_交易域`=2、`e2e_s7_订单`=2、`e2e_s7_退款`=0、`e2e_s7_用户域`=1

### 1.2 分类树过滤表格

- **操作步骤**：
    1. 点 `e2e_s7_交易域` → 表格只含 `e2e_s7_trade_orders` + `e2e_s7_trade_refunds`
    2. 展开交易域点 `e2e_s7_订单` → 同上；点 `e2e_s7_退款` → 空态
    3. 点 `e2e_s7_用户域` → 只含 `e2e_s7_user_profile`
    4. 点 `未分类` → 含 `e2e_s7_product_sku` + `e2e_s7_doris_mart`（不含已分类的 3 张）
    5. 点 `全部资产` → 回到全量浏览态
- **验收标准**：
    - [ ] 各节点过滤正确，计数徽章与结果一致
    - [ ] 空主题显示「该分类下暂无数据表」

### 1.3 浏览态筛选（数据源 / 健康度）

- **操作步骤**：
    1. 「按数据源筛选」选 `e2e_s7_mysql_ds` → 即时生效
    2. 「按健康度筛选」选 `差(BAD)` → 即时生效
    3. 清空两项筛选恢复
- **验收标准**：
    - [ ] 数据源=`e2e_s7_mysql_ds` 时含 T1~T4，不含 Doris 表 T5
    - [ ] 健康度=`BAD` 时含 T4（评分 20），不含 T1(优秀)/T2(良好)
    - [ ] 下拉筛选即时生效，无需点查询按钮

### 1.4 多维搜索

- **操作步骤**：
    1. 搜索框输入 `e2e_s7_trade` 点查询 → 只含 T1/T2，**搜索态无分页器**
    2. 输入 `电商` 点查询 → 注释命中 T1/T2，无 T3
    3. 输入 `交易流水号` 点查询 → 字段注释命中仅 T1
    4. 输入 `s7_analyst` 点查询 → 负责人命中仅 T1，行内负责人列显示 `s7_analyst`
    5. 输入 `zzz_no_such_keyword` 点查询 → 空态
    6. 点「重置」→ 回浏览态，搜索框清空
- **验收标准**：
    - [ ] 表名/注释/字段/负责人四类搜索均命中正确
    - [ ] 搜索无结果显示空态提示
    - [ ] 重置后分页器回归
    - [ ] 搜索态内仍可叠加数据源/健康度筛选（搜 `e2e_s7` 5 张全命中 → 数据源筛选后 T5 消失）

### 1.5 资产详情页

- **前置**：搜索 `e2e_s7_trade_orders` → 点行进详情（URL：`/asset-catalog/9000070000000000011`）
- **验收标准**：
    - [ ] 头部：表名、分类徽章 `e2e_s7_交易域`/`e2e_s7_订单`、负责人 `s7_analyst`
    - [ ] 三指标卡：`质量评分` 95 / `优秀` 徽章；`字段数` 3；`直接上游 / 下游表` 1 / 1
    - [ ] 基础信息页签：表全名 `testdb.e2e_s7_trade_orders`、数据域/主题 KV
    - [ ] 字段列表页签（懒加载）：3 列 `id`(bigint) / `trade_no`(varchar,交易流水号) / `amount`(decimal,订单金额)
    - [ ] 血缘图谱页签：当前表 + 上游 `testdb.e2e_s7_user_profile` + 下游 `testdb.e2e_s7_trade_refunds`
    - [ ] 点「查看完整血缘图谱」→ 跳转 `/governance/metadata/lineage?from=asset-catalog`；点「← 返回」回到详情页
    - [ ] 质量页签：启用规则数 3；通过/警告/严重 = 2/1/0；规则表 3 条含分级徽章；有「立即执行全部规则」按钮
    - [ ] `← 返回` 回资产目录首页

### 1.6 边界场景

- **操作步骤**：
    1. 访问 `/asset-catalog/9000070000000000014`（T4 无血缘）→「血缘图谱」tab
    2. 访问 `/asset-catalog/9000070000000000015`（T5 Doris 内置表）
- **验收标准**：
    - [ ] T4 血缘空态「暂无血缘数据」
    - [ ] T5 数据源回显「Doris 数仓」（兜底不显示空/未知）

### 1.7 分类维护（治理员写操作）

- **新增数据域**：
    - 填表数据：名称 `e2e_s7_临时域`（照填）
    - 验收：通知「分类已创建」→ 树出现；删除空域成功
- **新增主题**：先点 `e2e_s7_用户域` 出现管理条 →「新增主题」填 `e2e_s7_支付` → 树出现；删除空主题成功
- **编辑主题改名级联**：编辑 `e2e_s7_订单` → 改名 `e2e_s7_订单V2` → T1/T2 行徽章同步变新名；改回还原
- **删除拦截**：
    - 删除被引用的主题 `e2e_s7_订单` → 拦截（提示被引用）
    - 删除有子分类的域 `e2e_s7_交易域` → 拦截（提示含子分类）
- **批量分配**：点 `e2e_s7_用户域` →「分配表到此分类」→ 搜索勾选 `e2e_s7_product_sku`(T4) →「批量分配(1)」→ 用户域计数 1→2、T4 入表；行操作「移出当前分类」→ 计数 2→1
- **详情页分配分类**：T4 详情 →「分配分类」→ 选 `e2e_s7_用户域` → 徽章出现；再次进入清空数据域 → 徽章清除
- **配置负责人**：搜索 T2 → 行「配置负责人」→ 选 `s7_govadmin` → 行内显示；再次进入清除 → 空

### 1.8 权限隔离（API 快速验证，不切账号）

- **操作步骤**（终端执行，见下方命令）：
    1. 用 s7_engineer token 调读接口 → 应 200
    2. 用 s7_engineer / s7_analyst token 调写接口（新增分类/分配分类/配置负责人/批量分配）→ 应全部拒绝
- **验收标准**：
    - [ ] 读接口四角色可用
    - [ ] 写接口工程师/分析师一律拒绝

```bash
# 1) 工程师读（应 200）
TOKEN_E=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"s7_engineer","password":"Test123456"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")
echo "== 工程师读 =="
curl -s -H "Authorization: Bearer $TOKEN_E" \
  "http://localhost:8080/api/governance/assets/search?keyword=e2e_s7" \
  | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS(200)' if d['code']==200 else 'FAIL: '+str(d))"

# 2) 工程师写（应拒绝）
echo "== 工程师新增分类 =="
curl -s -X POST -H "Authorization: Bearer $TOKEN_E" -H 'Content-Type: application/json' \
  http://localhost:8080/api/governance/assets/classifications -d '{"level":"DOMAIN","name":"x"}' \
  | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS(rejected)' if d['code']!=200 else 'FAIL: 不应允许')"

# 3) 分析师写（应拒绝）
TOKEN_A=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"s7_analyst","password":"Test123456"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")
echo "== 分析师新增分类 =="
curl -s -X POST -H "Authorization: Bearer $TOKEN_A" -H 'Content-Type: application/json' \
  http://localhost:8080/api/governance/assets/classifications -d '{"level":"DOMAIN","name":"x"}' \
  | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS(rejected)' if d['code']!=200 else 'FAIL: 不应允许')"
```

---

## 二、F2 任务模板库

> 入口：`数据工程 → 任务模板库`（`/engineering/task-templates`）

### 2.1 列表与过滤

- **操作步骤**：
    1. 访问 `/engineering/task-templates`
- **验收标准**：
    - [ ] 标题「任务模板库」；3 条内置模板：`整表同步`(SYNC,内置,启用,创建人系统) / `增量同步（每日）`(SYNC,内置,启用) / `元数据全量采集`(COLLECT,内置,启用)
    - [ ] 底部说明条「模板被删除不影响已创建任务」
    - [ ] 分页器「共 3 条」
    - [ ] segmented 过滤：`同步任务` 只显 2 条 SYNC；`采集任务` 只显 COLLECT；`全部` 恢复

### 2.2 一键创建任务

- **SYNC 整表同步**：
    - 操作：点「整表同步」行「一键创建」→ 居中弹窗「从模板创建」
    - 填表数据（5 个占位符）：
        - 任务名称 `*`：`e2e_s7_task_full_sync`（照填）
        - `{source_datasource}`：选 `e2e_s7_mysql_ds`
        - `{source_db}`：`testdb`；`{source_table}`：`e2e_s7_src_orders`；`{target_db}`：`dwd`；`{target_table}`：`e2e_s7_tgt_full`
    - 负向：任务名留空 → 提示「请输入任务名称」；填了任务名但源数据源留空 → 提示「请填写「源数据源」」
    - 验收：通知「已创建同步任务」；DB 验证占位符替换、sync_mode=FULL
- **SYNC 增量同步**：
    - 填表数据：任务名称 `e2e_s7_task_incr_sync`；数据源同上；`{source_db}` `testdb`；`{source_table}` `e2e_s7_src_orders`；`{incremental_field}` `updated_at`；`{target_db}` `dwd`；`{target_table}` `e2e_s7_tgt_incr`
    - 验收：schedule_cron 预填 `0 0 2 * * ?`（非必填）；落库 sync_mode=INCREMENTAL、cron 正确
- **COLLECT 元数据全量采集**：
    - 填表数据：任务名称 `e2e_s7_task_collect`；数据源 `e2e_s7_mysql_ds`；采集范围 `testdb`
    - 验收：通知「已创建采集任务」；`collect_task`（governance 库）落库

### 2.3 模板 CRUD

- **新增自定义模板（手动 JSON）**：
    - 填表数据：
        - 模板名称 `*`：`e2e_s7_custom_sync`（照填）
        - 任务类型 `*`：选「同步任务」
        - JSON 配置（照填）：
          ```json
          {
            "placeholders": [{"key": "target_table", "label": "目标表", "required": true}],
            "config": {
              "sourceDatasourceId": "1", "sourceDatabase": "testdb",
              "sourceTables": ["e2e_s7_src_orders"], "syncMode": "FULL",
              "triggerType": "MANUAL", "targetDatabase": "dwd",
              "targetTable": "{target_table}"
            }
          }
          ```
    - 验收：通知「模板已创建」；列表出现「自定义」徽章行，创建人 `admin`，有编辑/删除按钮
- **重名校验**：再建同名 → 拦截（7302「已存在」），抽屉不关闭
- **编辑模板**：点编辑 → 类型下拉锁定（disabled）；改名 `e2e_s7_renamed_tpl` 保存 → 列表刷新
- **复制内置为自定义**：点「整表同步」复制 → 名称预填「整表同步 副本」、JSON 预填；改名 `e2e_s7_copied_tpl` 保存 → 出现「自定义」徽章
- **删除自定义模板**：删除 → 确认 → 行消失；内置模板行无编辑/删除按钮（只有一键创建 + 复制）
- **从已配置任务另存**：新增自定义模板 → 名称 `e2e_s7_saveas_tpl` →「从已配置任务另存」下拉选 `e2e_s7_tpl_source_sync` → JSON 文本框隐藏；保存后列表出现该模板，占位参数列含 `{source_table}`

---

## 三、F3 子 DAG 参数下发

> 入口：`数据工程 → 项目管理 → DAG 编辑画布`
> **说明**：需预先创建父子 DAG 夹具（见下），或复用已有带 SUB_DAG 节点的 DAG。

### 3.0 前置：创建测试 DAG 夹具（API 一键）

```bash
TOKEN=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")

PJ=$(curl -s -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  http://localhost:8080/api/engineering/dev/dag-projects -d '{"name":"e2e_s7_f3_project"}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['id'])")

SUB=$(curl -s -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  http://localhost:8080/api/engineering/dev/dags \
  -d "{\"projectId\":\"$PJ\",\"name\":\"e2e_s7_f3_sub\",\"triggerType\":\"MANUAL\",\"scheduleEnabled\":false,\"maxParallelism\":1,\"status\":\"ENABLED\",\"nodes\":[{\"nodeId\":\"s1\",\"nodeName\":\"子SQL\",\"nodeType\":\"SQL\",\"positionX\":0,\"positionY\":0,\"config\":\"{\\\"type\\\":\\\"SQL\\\",\\\"sqlContent\\\":\\\"select 1\\\"}\"}],\"edges\":[]}" \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['id'])")

curl -s -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  http://localhost:8080/api/engineering/dev/dags/$SUB/parameters -d '{"paramName":"sub_date","paramType":"STRING","required":false}' > /dev/null
curl -s -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  http://localhost:8080/api/engineering/dev/dags/$SUB/parameters -d '{"paramName":"sub_env","paramType":"STRING","required":false}' > /dev/null

PARENT=$(curl -s -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  http://localhost:8080/api/engineering/dev/dags \
  -d "{\"projectId\":\"$PJ\",\"name\":\"e2e_s7_f3_parent\",\"triggerType\":\"MANUAL\",\"scheduleEnabled\":false,\"maxParallelism\":1,\"status\":\"ENABLED\",\"nodes\":[{\"nodeId\":\"p1\",\"nodeName\":\"前置SQL\",\"nodeType\":\"SQL\",\"positionX\":0,\"positionY\":0,\"config\":\"{\\\"type\\\":\\\"SQL\\\",\\\"sqlContent\\\":\\\"select 1\\\"}\"},{\"nodeId\":\"p2\",\"nodeName\":\"异步子DAG\",\"nodeType\":\"SUB_DAG\",\"positionX\":240,\"positionY\":0,\"config\":\"{\\\"type\\\":\\\"SUB_DAG\\\",\\\"subDagId\\\":\\\"$SUB\\\",\\\"subDagName\\\":\\\"e2e_s7_f3_sub\\\",\\\"syncExecution\\\":false}\"}],\"edges\":[{\"edgeId\":\"e1\",\"sourceNodeId\":\"p1\",\"targetNodeId\":\"p2\"}]}" \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['id'])")

curl -s -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  http://localhost:8080/api/engineering/dev/dags/$PARENT/parameters -d '{"paramName":"main_env","paramType":"STRING","defaultValue":"prod","required":false}' > /dev/null

echo "PROJECT=$PJ"; echo "SUB=$SUB"; echo "PARENT=$PARENT"
```

### 3.1 参数映射编辑器 UI

- **操作步骤**：
    1. `/engineering/dags` → 进入项目 `e2e_s7_f3_project` → 打开 `e2e_s7_f3_parent` 编辑画布
    2. 双击画布中「异步子DAG」节点（p2）→ 打开「子 DAG 配置」弹窗
- **验收标准**：
    - [ ] 弹窗含「参数下发（可选）」区域，有「添加映射」按钮
    - [ ] 点「添加映射」→ 出现映射行：主参数下拉 + 子参数输入
    - [ ] 主参数下拉候选含：`main_env`（父 DAG 声明参数）+ `biz_date`/`current_time`/`dag_id`（系统变量）
    - [ ] 子参数输入可填子 DAG 声明参数名（`sub_date` / `sub_env`）

### 3.2 前端校验

- **操作步骤**：
    1. 添加映射：主参数选 `biz_date`，子参数留空 → 点保存
    2. 填子参数 `sub_date` → 再添加第二条映射：主参数 `main_env`、子参数也填 `sub_date` → 点保存
- **验收标准**：
    - [ ] 子参数空 → 弹窗提示「存在未填写子 DAG 参数的映射行」
    - [ ] 两条映射子参数重复 → 提示「在映射中重复」

### 3.3 保存持久化 + 回显 + 删除

- **操作步骤**：
    1. 添加映射 1：`biz_date → sub_date`；映射 2：`main_env → sub_env` → 弹窗保存 → 保存 DAG
    2. 重开弹窗 → 映射回显
    3. 删除映射 2 → 保存 DAG → 重开只剩 1 条；删除映射 1 → 保存 → 弹窗无映射
- **验收标准**：
    - [ ] 保存后节点 config 含 `paramMappings` 且字段完整（可查 `/engineering/dev/dags/{PARENT}` 的 p2.config）
    - [ ] 重开回显正确
    - [ ] 删除后 config 不含 `paramMappings`（还原）

### 3.4 执行链路（真实触发，主流程）

- **前置**：父 DAG 已配 `main_env → sub_env` 映射；子 DAG 参数 `sub_env` 默认 `sub_default_env`、`sub_date` 无默认值
- **操作步骤**：
    1. `/engineering/dag-executions` → 找到父 DAG `e2e_s7_f3_parent` → 点「立即执行」（或经 `/engineering/dev/dags/{PARENT}/trigger` API）
    2. 轮询至父 DAG 执行终态 SUCCESS；查看子 DAG `e2e_s7_f3_sub` 也产生新执行
- **验收标准**：
    - [ ] 父 DAG 执行 SUCCESS；子 DAG 产生新执行且 SUCCESS
    - [ ] 子执行 `dag_execution.resolved_params` 含 `sub_env`，值来自父执行 `main_env` 的解析值（默认 `prod`）
    - [ ] 异步链路子执行 `trigger_type=MANUAL`

### 3.5 执行链路（同步子 DAG + 无值边界）

- **同步链路**：新建同步执行 SUB_DAG 的父 DAG（`syncExecution: true`）→ 执行 → 子执行 `resolved_params` 收到映射值；同步链路子执行 `trigger_type=SCHEDULED`
- **无值边界**：主参数声明但无默认值且触发不传 → 父/子均执行成功（warn 跳过不阻断），子参数用子 DAG 自身默认值（`sub_default_env`）
- **回归**：无 `paramMappings` 时子 DAG 按原语义执行（只带系统变量/默认值，无透传值）

---

## 四、F4 Python 质量规则 + CUSTOM_SQL

> 入口：`数据治理 → 质量规则`（`/governance/quality-rules`）；`数据治理 → 规则模板库`（`/governance/quality-templates`）
> **注意**：Python 规则「测试脚本」和 CUSTOM_SQL「执行预览」需要**真实可连的数据源**。种子数据源 `e2e_s7_mysql_ds` 密码是假的连不上，**请用环境存量 `mysql` 数据源（testdb）**。

### 4.1 规则模板库：PYTHON 模板

- **前置**：确认模板库页面有 `Python 自定义检查`（或手动新增 PYTHON 模板）
- **新增 PYTHON 模板**：
    - 填表数据：
        - 模板名称 `*`：`e2e_s7_f4_py_tpl`（照填）
        - 模板类型 `*`：切 **Python**
        - 脚本区出现 `textarea[placeholder="def check(df): ..."]`（SQL 模板区隐藏）
        - 脚本内容（照填）：
          ```python
          def check(df):
              if df.empty:
                  return {'null_rate': 0.0}
              return {'null_rate': float(df['total_amount'].isnull().mean())}
          ```
        - 结果指标名 `*`：`null_rate`
    - 验收：保存 → 列表出现（自定义徽章）；DB `quality_rule_template.python_template` 含 `def check`；编辑回显脚本保留；删除成功

### 4.2 PYTHON 规则

- **新增 PYTHON 规则**：
    - 填表数据：
        - 规则名称 `*`：`e2e_s7_f4_py_rule`（照填）
        - 规则类型 `*`：选 **Python**
        - 数据源 `*`：选 `mysql`（真实可连）；库 `testdb`；表 `orders`（或 `e2e_s7_trade_orders`）
        - 检查字段：可选（无必填星号）
        - Python 脚本 `*`：`def check(df): return {'null_rate': 0.0}`
        - 结果指标名 `*`：`null_rate`
    - **测试脚本**：点「测试脚本」→ 真实沙箱试跑，60s 内返回 dict（含 `null_rate` 键）→「执行成功」
    - 负向：脚本留空 → 提示「请输入 Python 校验脚本」；脚本有但结果指标空 → 提示「请输入结果指标名」
    - 验收：保存成功；DB `quality_rule.type=PYTHON`、`python_script` 落库；编辑回显脚本保留；删除成功

### 4.3 CUSTOM_SQL 执行预览

- **操作步骤**：
    1. 新增规则 → 类型选「自定义 SQL」→ 数据源 `mysql` / `testdb` / `orders`
    2. 校验 SQL（照填）：`SELECT COUNT(*) AS total, SUM(CASE WHEN total_amount IS NULL THEN 1 ELSE 0 END) AS nulls FROM {table}`
    3. 点「执行预览」
- **验收标准**：
    - [ ] 预览返回样例行（「Query returned ...」），展示 `total` / `nulls` 两个指标列 chips
    - [ ] 点 `nulls` chip → 结果指标名自动回填 `nulls`
    - [ ] 执行失败时展示错误信息（不白屏）

---

## 五、§6.5 收尾补强（质量任务调度语义回归）

> 入口：`数据治理 → 质量任务`（`/governance/data-quality`）
> **说明**：本节为 2026-08-09 后端修复（app-governance 已重建），无自动化覆盖，**重点手动验证**。

### 5.1 质量任务删除 RUNNING 保护

- **前置**：新建质量任务（含 1 条规则，表选真实表如 `orders`），点「执行」使其处于 RUNNING 状态
- **操作步骤**：
    1. 任务执行中（批次 `quality_check_batch.status='RUNNING'`）尝试删除该任务
    2. 任务执行完成后（批次终态）再删除
- **验收标准**：
    - [ ] RUNNING 中删除被拦截，提示含「运行中/4218」类文案（ErrorCode 4218）
    - [ ] 执行完成后删除成功

### 5.2 创建 cron 任务不自动开启调度

- **操作步骤**：
    1. 新建质量任务：触发方式选「Cron 定时」，cron 填 `0 0 2 * * ?`，**不勾**「开启定时调度」→ 保存
    2. 查看任务列表调度列
    3. 进入任务详情点「启动调度」
- **验收标准**：
    - [ ] 保存后 `scheduled_enabled=0`（未开启调度），PowerJob 控制台**不**出现该任务的 active 调度（或已注册但未启动）
    - [ ] 手动「启动调度」后真正生效（PowerJob 该任务为已启动状态）

### 5.3 编辑任务调度同步一致性

- **操作步骤**：
    1. 对 5.2 的任务：编辑改为「手动触发」再改回「Cron 定时」→ 保存
    2. 对已开启调度的任务：修改 cron 表达式 → 保存
- **验收标准**：
    - [ ] 编辑后 DB `scheduler_job_id` / `cron_expression` 与 PowerJob 控制台一致（无「DB 显示已开但调度中心未注册」的错位）

---

## 执行建议

1. **环境**：前端统一走网关 `http://localhost:8080`（`/api/<服务>/...`）；PowerJob 控制台 `http://localhost:7700`。
2. **执行顺序**（推荐按数据流）：
   - 先播种（§0.4）→ 验证播种（§0.5）
   - F1 资产目录（浏览/搜索/详情/血缘/质量/分类维护）
   - F2 任务模板库（一键创建 + CRUD）
   - F3 子 DAG 参数下发（先建夹具 → 配置 → 执行链路）
   - F4 Python 规则 + CUSTOM_SQL（用真实 mysql 数据源）
   - §6.5 收尾补强（质量任务调度语义）
3. **填表数据速查**：
   - 资产目录测试表：`e2e_s7_trade_orders`(95/优秀) / `e2e_s7_trade_refunds`(85/良好) / `e2e_s7_user_profile`(70/一般) / `e2e_s7_product_sku`(20/差) / `e2e_s7_doris_mart`(Doris 内置)
   - 分类：`e2e_s7_交易域`（订单/退款）+ `e2e_s7_用户域`
   - 质量规则（T1 详情页展示）：`e2e_s7_订单完整性`(PASS) / `e2e_s7_订单唯一性`(PASS) / `e2e_s7_金额范围`(WARNING)
   - 模板库：内置 `整表同步` / `增量同步（每日）` / `元数据全量采集`
   - Python/CUSTOM_SQL 用真实数据源 `mysql`（testdb）的 `orders` 表
4. **重点回归**：
   - 资产详情「血缘空态」「Doris 兜底回显」
   - 分类改名级联同步到表徽章（F1-37）
   - 删除被引用主题 / 有子分类域的拦截
   - 一键创建占位符必填校验（源数据源是第一个必填）
   - §6.5：RUNNING 删除保护 + cron 不自动开启调度 + 编辑调度一致性
