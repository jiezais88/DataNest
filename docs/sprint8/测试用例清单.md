# DataNest Sprint 8 人工验收清单

> **验收方式**：页面上人工操作，按「填表数据」照填，逐项确认验收标准
> **范围**：Sprint 8（F1 资产目录深化 + F2 实时 CDC 管道 + F3 质量报告 + 导出/时间格式收尾）
> **账号**：admin 主链路；权限用 API 快速验证（s7_analyst / s7_engineer / s7_govadmin，密码均 Test123456）
> **自动化状态（2026-08-11）**：E2E **59 passed**（F1 18 + F2 23 + F3 18）已全部跑通——本清单只做核心链路人工确认，边缘场景已由自动化覆盖，不重复罗列
> **种子数据**：**已执行完毕，开箱即验**（重播 SQL 见附录 A，数据被破坏时才需要）

---

## 0. 环境确认

| 资源 | 地址 | 账号 |
|------|------|------|
| 前端页面 | http://localhost:3000 | admin / admin123 |
| API 网关 | http://localhost:8080/api/... | - |
| Doris 湖仓查询 | `docker exec -i datanest-middleware-mysql mysql -h192.168.119.135 -P9030 -uroot -ppassword` | root / password |
| Flink Web UI | http://localhost:18081 | - |

- [ ] `docker compose ps` 全部 Up/healthy；`http://localhost:3000` 可登录

```bash
# 取 Token（API 用例用）
TOKEN=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")
```

---

## 用例汇总（24 个）

| 功能块 | 用例数 | 核心确认点 |
|--------|--------|-----------|
| F1 资产目录深化 | 8 | 标签/收藏/关注/评论/热度主链路 |
| F2 实时 CDC 管道 | 8 | 向导→快照→增量→savepoint 恢复→编辑删除 |
| F3 质量报告 | 5 | KPI/联动/趋势/清单/导出 |
| 权限与导出专项 | 3 | 权限矩阵 + 三件套导出口径 |

---

## 一、F1 资产目录深化（8）

> 入口：侧边栏「数据资产」。以下均在 `e2e_s7_trade_orders`（T1，95 分/优秀）上操作。

### 1.1 打标签 / 删标签

1. T1 详情页 → 头部「添加标签」→ 输入 `核心表` 回车；再重复添加一次 `核心表`
2. 再添加 `高可用` → hover 点 × 移除

- [ ] 标签芯片展示；重复打不重复（幂等）；移除后消失并提示「已移除标签」

### 1.2 标签云筛选 + 搜索命中 + chip 跳转

1. 回资产首页 →「标签筛选」行点 `核心表` → 只剩 T1，行内标签列有芯片（列表里的标签 chip 也可直接点击筛选）
2. 搜索框输入 `核心` 点查询 → T1 命中（标签名搜索）
3. T1 详情页**头部标签区**点 `核心表` chip（hover 有下划线/提示「按标签筛选」）→ 跳回目录并自动按该标签筛选（标签筛选行 chip 紫色选中态高亮）

- [ ] 三种入口的标签筛选都命中正确

### 1.3 收藏 + 我的收藏页

1. T1 详情页点「收藏」→ 变「已收藏」
2. 侧边栏「我的收藏」→ 列表含 T1（收藏时间/评分）；关键词 `trade_orders` 查询命中、「重置」恢复；健康度选「优秀」查询命中
3. 行内「取消收藏」→ 确认 → 行消失（**之后重新收藏回 T1**）

- [ ] 收藏开关、列表、筛选、取消全链路正确

### 1.4 收藏导出 Excel

- 「我的收藏」页点「导出」→ 下载 `DataNest-我的收藏-*.xlsx`，Excel 打开

- [ ] 列宽自适应（无 `#####`）；表头含「表名/注释/数据源/…/收藏时间」；收藏时间是 `yyyy-MM-dd HH:mm:ss`

### 1.5 关注 + 变更动态

1. T1 详情页点「关注」→ 变「已关注」
2. 「我的关注」页 → T1 行有「元数据变更」摘要区（无变更时为空态文案）
3. 行内「取消关注」→ 确认 → 行消失（**之后重新关注回 T1**）

- [ ] 关注开关、动态摘要、取消全链路正确

### 1.6 评论

1. T1 详情 →「评论」页签 → 填 `每日 00:30 更新，可放心使用` →「发表」→ 列表出现（用户名+时间）、页签计数 +1
2. 点「删除」→ 确认 → 消失
3. 用 s7_govadmin 重新登录发一条评论；用 s7_engineer 登录查看 → 无删除按钮；s7_govadmin 可删

- [ ] 发表/自删/计数正确；工程师无删除按钮、治理员可删他人

### 1.7 热度

1. 打开任意表详情 → 第 4 张指标卡「热度（近 30 天访问）」数值 +1；同会话刷新不再 +1
2. 资产首页「热门数据表」面板（可折叠）列出访问过的表；排序下拉切「按热度」/「按最新」生效

- [ ] 埋点计数、热门面板、两种排序正确

### 1.8 菜单可见性

- [ ] 三个测试角色侧边栏均可见「我的收藏」「我的关注」

---

## 二、F2 实时 CDC 管道（8）

> 入口：`数据工程 → CDC 管道`。真实链路：MySQL/PG → Flink CDC → Iceberg（MinIO）→ Doris。
> **注意**：集群仅 1 个 Task Slot，**串行启停**；启动/停止后给系统 15~30 秒；Doris 查不到新数据先点行内「刷新 Catalog」。

### 2.1 新建向导

1. 「新建管道」→ ① 基本信息：名称 `验收_用户实时同步`（照填），描述可选
2. ② 配置管道：源数据源 `mysql（middleware-test-mysql）` → 源数据库 `testdb` → 勾选 `e2e_s8_cdc_users`（主键自动回填 `id`）→ 目标库输入 `s8_accept_dwd`（照填）→ 同步模式/写入模式保持默认（全量+增量 / Upsert）→ 高级配置 Checkpoint 选「实时（10 秒）」，并行度保持 1
3. ③ 确认启动：预检自动执行 → 点「仅保存」

- [ ] 预检 4 项全绿（连通/binlog/ROW/库存在）
- [ ] 并行度下方动态提示「当前集群 1 个 Task Slot（空闲 N）…」（实时检测，非写死）
- [ ] Checkpoint 为档位选择（实时 10 秒/准实时 60 秒默认/低优先 10 分钟/自定义），确认页摘要显示「实时（10 秒）」、表映射与 Doris 查询入口正确
- [ ] 保存后列表出现该行：状态「已停止」、创建人/时间有值

### 2.2 列表页 + 详情抽屉

- [ ] 4 统计卡（运行中/已停止/异常/已同步表）数字正确；状态分段即时过滤；关键词搜索/重置正常
- [ ] 「详情」抽屉五区完整（基本/源/目标含表映射主键/运行状态/高级配置）

### 2.3 启动 → 全量快照落湖（核心）

1. 行内点「启动」→ 状态「运行中」、出现运行时长
2. 等 ~30 秒 → 点「刷新 Catalog」→ 终端查湖仓：

```bash
docker exec -i datanest-middleware-mysql mysql -h192.168.119.135 -P9030 -uroot -ppassword \
  -e "SELECT * FROM datalake_catalog.s8_accept_dwd.e2e_s8_cdc_users ORDER BY id"
```

- [ ] Doris 返回 3 行（seed_a/b/c），湖仓自动建表、数据与源一致

### 2.4 增量同步秒级可见

```bash
docker exec -i datanest-middleware-test-mysql mysql -u root -proot123 testdb \
  -e "INSERT INTO e2e_s8_cdc_users (username, amount) VALUES ('accept_d', 400), ('accept_e', 500)"
```

等 ~40 秒（checkpoint 10s 落湖 + 平台每 30s 自动刷新 Doris catalog）→ 直接查 Doris（**无需手动点刷新**）

- [ ] Doris 自动变 5 行（30 秒内）；列表「累计变更」≥5、「当前延迟」有值
- [ ] 手动「刷新 Catalog」按钮仍可立即刷新（立等可取场景）

### 2.5 日志抽屉

- [ ] 含「管道创建」「管道启动成功，Flink 作业 ID: …」条目；运行中有「自动刷新（5s）」；「清屏」/「刷新」行为正确

### 2.6 停止 → savepoint 恢复不丢不重（核心）

1. 点「停止」→ 确认 → 状态「已停止」
2. 停止期间插 1 行：

```bash
docker exec -i datanest-middleware-test-mysql mysql -u root -proot123 testdb \
  -e "INSERT INTO e2e_s8_cdc_users (username, amount) VALUES ('accept_f_during_stop', 600)"
```

3. 点「启动」（从 savepoint 恢复）→ 等 ~30 秒 → 刷新 Catalog → 查 Doris

- [ ] Doris 精确 6 行、id 无重复（不丢不重）；详情抽屉 Savepoint 字段有 `s3a://` 路径
- [ ] 列表「运行时长」为自适应粒度（<1 分钟显示 `N 秒`，之后 `N 分 N 秒` / `N 小时 N 分`）

### 2.7 编辑 → 删除

1. 点「编辑」→ 向导预填（Checkpoint 回显「实时（10 秒）」档）；改描述 `验收用（已编辑）`、Checkpoint 改「自定义秒数…」填 `15` → 保存
2. 列表点「删除」→ 确认（提示级联清理、湖仓保留）→ 行消失

- [ ] 编辑后：描述更新、savepoint 清空、高级配置 15 秒、修改人/时间有值
- [ ] 运行中时「编辑/删除」按钮禁用；删除后统计卡联动、Doris 湖仓数据仍可查（保留）

### 2.8 仅增量 + PG 源（快验，可合并观察）

1. 新建管道：表 `e2e_s8_cdc_incr`、目标库 `s8_accept_dwd`、同步模式「仅增量」→ 启动位点自动「从最新」→ 保存并启动；30 秒后插 2 行新数据 → 刷新 Catalog → 查湖仓
2. 再开新建向导：源选 PG 数据源（middleware-test-postgres）→ 选「仅增量」→「从最早」为禁用态（提示 connector 不支持）；取消不保存

- [ ] 仅增量管道湖仓表只含新插入行（old_1~3 存量未同步）；验完停止并删除该管道
- [ ] PG 源「从最早」禁用提示正确

---

## 三、F3 质量报告（5）

> 入口：`数据治理 → 质量报告`。以下先把「按数据源筛选」选为 `e2e_s7_mysql_ds` 再点「查询」，数值即与预期精确对应。

### 3.1 KPI + 页面结构

- [ ] 5 张 KPI 卡：检查批次 `2` / 规则明细 `15` / 平均评分 `67.5` / 通过率 `13.33%` / 待处理问题 `13`（严重 5 / 警告 8，红色）
- [ ] 图表卡齐全：四档分布趋势 / 平均评分趋势 / 表评分分布 / 数据源质量对比 / 问题清单
- [ ] 筛选改动是草稿，点「查询」才刷新

### 3.2 筛选联动

1. 选数据源 `e2e_s7_mysql_ds` → 库下拉只剩 `testdb（e2e_s7_mysql_ds）`、任务下拉只剩 `e2e_s8_报告任务`
2. 数据源改回「全部」→ 库下拉选 `datanest（Doris 数仓）` → 数据源自动带出、任务下拉提示「该数据源下暂无任务」
3. 时间范围选「自定义」不选起止点「查询」→ 提示「请选择自定义起止时间」

- [ ] 库选项是 `库名（数据源名）` 格式（同名库无歧义）；选库精确带出数据源；空态/校验提示正确

### 3.3 趋势与分布

- [ ] 四档趋势当天：通过 2 / 警告 8 / 严重 5；平均评分趋势 3 个点（80 → 90 → 90）
- [ ] 评分分布环图：优秀/良好/一般/差各 1 张（共 4 张）；数据源对比 `e2e_s7_mysql_ds` 均分 67.5

### 3.4 问题清单

- [ ] TOP6 最新在前；行内含阈值（警告 0.1 / 严重 0.5）
- [ ] 「查看全部 13 条」抽屉第 2 页 3 条（含 `e2e_s7_金额范围`）；点 TOP6 行跳该表详情「质量」页签

### 3.5 导出 Excel（治理员/超管）

- 点「导出」→ Excel 打开 `DataNest-质量报告-*.xlsx`

- [ ] 列宽自适应（无 `#####`）；汇总段 + 问题清单全量段齐全
- [ ] 枚举全中文（类型「完整性/自定义 SQL」、级别「严重/警告」）；时间 `yyyy-MM-dd HH:mm:ss`
- [ ] 规则名为 `=HYPERLINK(...)` 的行原样存储（xlsx 字符串单元格不执行公式）

---

## 四、权限与导出专项（3）

### 4.1 CDC 管道权限（API 快验）

```bash
TOKEN_A=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"s7_analyst","password":"Test123456"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")
echo "== 分析师读（应 200）=="
curl -s -H "Authorization: $TOKEN_A" "http://localhost:8080/api/realtime/cdc/pipelines/stats"
echo; echo "== 分析师写（应 1005）=="
curl -s -X POST -H "Authorization: $TOKEN_A" -H 'Content-Type: application/json' \
  http://localhost:8080/api/realtime/cdc/pipelines/validate-source -d '{"datasourceId":"2083088527209295874"}'
```

- [ ] 分析师读 200 / 写 1005；UI 无「新建管道」按钮与写操作图标
- [ ] 删数据源被引用拦截：`curl -X DELETE http://localhost:8080/api/engineering/datasources/2083088527209295874 -H "Authorization: $TOKEN"` → 8009（**mysql 是公共测试源，出现 8009 即通过，不要真删**）

### 4.2 报告导出权限（API 快验）

```bash
curl -s -X POST -H "Authorization: $TOKEN_A" -H 'Content-Type: application/json' \
  http://localhost:8080/api/governance/quality/report/export -d '{}'
```

- [ ] 分析师/工程师导出 → 1005；治理员/超管可导出（3.5 已验）
- [ ] 补算幂等（可选）：治理员调 `POST /governance/quality/report/backfill-score-history`，第二次返回 0

### 4.3 评论越权（API 快验）

```bash
TOKEN_E=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"s7_engineer","password":"Test123456"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")
CID=$(docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_governance -t -A \
  -c "SELECT id FROM asset_comment WHERE deleted = 0 ORDER BY id DESC LIMIT 1")
curl -s -X DELETE -H "Authorization: $TOKEN_E" "http://localhost:8080/api/governance/assets/comments/$CID"
```

- [ ] 工程师删他人评论 → 4023（1.6 已验 UI 隐藏与治理员可删）

### 4.4 标准合规导出

- **操作**：`数据治理 → 标准合规` →「导出」→ Excel 打开 xlsx

- **验收**：
    - [ ] 列宽自适应（无 `#####`）；对象类型「表名/字段名」、违规类型「命名规范/字段类型」全中文；检查时间 `yyyy-MM-dd HH:mm:ss`

---

## 附录 A：种子数据重播（数据被破坏时才需要）

> 当前种子已由测试会话执行完毕。重播顺序：① → ② → ③ → ④，全部幂等。

### ① 测试用户（走 API，密码由服务端加密）

```bash
TOKEN=$(curl -s -X POST http://localhost:8080/api/system/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['data']['token'])")
for u in govadmin:GOVERNANCE_ADMIN engineer:DATA_ENGINEER analyst:DATA_ANALYST; do
  name="s7_${u%%:*}"; role="${u##*:}"
  curl -s -X POST http://localhost:8080/api/system/users -H "Authorization: $TOKEN" \
    -H 'Content-Type: application/json' \
    -d "{\"username\":\"$name\",\"password\":\"Test123456\",\"roles\":[\"$role\"],\"email\":\"$name@test.io\"}"
done
# 已存在时返回「用户名已存在」属正常
```

### ② sprint7 基础数据

执行 `docs/sprint7/测试用例清单.md` §0.4 的 播种②（数据源）+ 播种③（资产目录全套）两个 SQL 块。

### ③ F3 质量报告数据 `datanest_governance`

```bash
docker exec -i datanest-middleware-postgres psql -U datanest -d datanest_governance <<'EOSQL'
DELETE FROM quality_score_history WHERE table_id IN (9000070000000000011,9000070000000000012);
DELETE FROM quality_check_detail WHERE batch_id = 9000080000000000061 OR rule_name LIKE 'e2e_s8%' OR rule_name LIKE '=HYPERLINK%';
DELETE FROM quality_check_batch WHERE id = 9000080000000000061;
DELETE FROM quality_rule WHERE id = 9000080000000000111 OR name LIKE 'e2e_s8%';
DELETE FROM quality_job WHERE id = 9000080000000000121 OR name LIKE 'e2e_s8%';
INSERT INTO quality_job (id, name, datasource_id, enabled, created_at)
VALUES (9000080000000000121, 'e2e_s8_报告任务', 9000070000000000001, 1, now());
INSERT INTO quality_rule (id, job_id, name, type, table_id, column_name, check_field, sql_expression, result_metric, warning_threshold, severe_threshold, weight, enabled, created_at, updated_at)
VALUES (9000080000000000111, 9000080000000000121, 'e2e_s8_报告阈值规则', 'COMPLETENESS', 9000070000000000011, 'amount', 1, 'SELECT 0 AS null_rate', 'null_rate', 0.1, 0.5, 1, 1, now(), now());
INSERT INTO quality_check_batch (id, job_id, job_name, trigger_type, status, started_at, ended_at, duration_ms, created_at, alert_sent)
VALUES (9000080000000000061, 9000080000000000121, 'e2e_s8_报告批次', 'MANUAL', 'SUCCESS', now(), now(), 100, now(), 0);
INSERT INTO quality_check_detail (id, batch_id, rule_id, rule_name, rule_type, table_id, result_metric, result_value, success, executed_sql, created_at, result_level)
SELECT 9000080000000000300 + i, 9000080000000000061, 9000080000000000111,
       CASE WHEN i = 6 THEN '=HYPERLINK("http://evil","click")' ELSE 'e2e_s8_问题规则_' || i END,
       'COMPLETENESS', CASE WHEN i % 2 = 1 THEN 9000070000000000011 ELSE 9000070000000000012 END,
       'null_rate', 0.5, 1, 'SELECT 0 AS null_rate', now(),
       CASE WHEN i <= 7 THEN 'WARNING' ELSE 'SEVERE' END
FROM generate_series(1, 12) AS i;
INSERT INTO quality_score_history (id, table_id, table_name, datasource_id, score, health_level, pass_rules, warning_rules, severe_rules, checked_at, created_at) VALUES
(9000080000000000201, 9000070000000000011, 'testdb.e2e_s7_trade_orders',  9000070000000000001, 80.00, 'GOOD',      2, 1, 0, now() - interval '2 days', now()),
(9000080000000000202, 9000070000000000011, 'testdb.e2e_s7_trade_orders',  9000070000000000001, 90.00, 'EXCELLENT', 3, 0, 0, now() - interval '1 days', now()),
(9000080000000000203, 9000070000000000011, 'testdb.e2e_s7_trade_orders',  9000070000000000001, 95.00, 'EXCELLENT', 3, 0, 0, now(), now()),
(9000080000000000204, 9000070000000000012, 'testdb.e2e_s7_trade_refunds', 9000070000000000001, 85.00, 'GOOD',      2, 0, 0, now(), now());
EOSQL
```

### ④ F2 CDC 源表

```bash
docker exec -i datanest-middleware-test-mysql mysql -u root -proot123 testdb <<'EOSQL'
DROP TABLE IF EXISTS e2e_s8_cdc_users;
CREATE TABLE e2e_s8_cdc_users (id BIGINT NOT NULL AUTO_INCREMENT, username VARCHAR(64) NOT NULL,
  amount INT DEFAULT 0, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO e2e_s8_cdc_users (username, amount) VALUES ('seed_a', 100), ('seed_b', 200), ('seed_c', 300);
DROP TABLE IF EXISTS e2e_s8_cdc_incr;
CREATE TABLE e2e_s8_cdc_incr (id BIGINT NOT NULL AUTO_INCREMENT, note VARCHAR(128), PRIMARY KEY (id));
INSERT INTO e2e_s8_cdc_incr (note) VALUES ('old_1'), ('old_2'), ('old_3');
EOSQL

docker exec -i datanest-middleware-test-postgres psql -U postgres -d postgres <<'EOSQL'
DROP TABLE IF EXISTS e2e_s8_pg_users;
CREATE TABLE e2e_s8_pg_users (id BIGINT PRIMARY KEY, name VARCHAR(64), updated_at TIMESTAMP DEFAULT now());
ALTER TABLE e2e_s8_pg_users REPLICA IDENTITY FULL;
INSERT INTO e2e_s8_pg_users (id, name) VALUES (1, 'pg_seed_a'), (2, 'pg_seed_b');
EOSQL
```

## 执行建议

- **顺序**：F1（纯 UI，最快）→ F3 → F2（真实 CDC 链路最耗时，放最后，严格串行启停）
- **核心验收点**：2.3 快照落湖、2.6 savepoint 恢复不丢不重、3.1 KPI 精确值、4.x 权限矩阵
- **验收后清理（可选）**：Doris `DROP TABLE datalake_catalog.s8_accept_dwd.*`；源库 `e2e_s8_*` 表可保留备查
