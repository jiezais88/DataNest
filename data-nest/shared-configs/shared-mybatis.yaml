# ============================================
# shared-mybatis.yaml
# MyBatis-Plus 共享配置
# 消费方: system-service / engineering-service / governance-service
# ============================================
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id
