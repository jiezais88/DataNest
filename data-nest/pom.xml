<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.datanest</groupId>
    <artifactId>data-nest</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>DataNest</name>
    <description>DataNest - Open Source Data Platform</description>

    <modules>
        <module>data-nest-libs</module>
        <module>data-nest-apis</module>
        <module>data-nest-services</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Framework versions -->
        <spring-boot.version>4.0.7</spring-boot.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.1.0.0</spring-cloud-alibaba.version>

        <!-- Library versions -->
        <sa-token.version>1.45.0</sa-token.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <flyway.version>10.22.0</flyway.version>
        <commons-pool2.version>2.12.0</commons-pool2.version>
        <!-- CSV 导出统一框架（Sprint 8 F3 起，导出规范见 conventions-backend §8） -->
        <commons-csv.version>1.14.1</commons-csv.version>
        <!-- springdoc 3.x 线适配 Spring Boot 4（2.x 线仅支持 Boot 3；Knife4j 4.5.0 未适配 Boot 4 未采用） -->
        <springdoc.version>3.0.2</springdoc.version>
        <!-- 与 springdoc 3.0.2 传递引入的 swagger-annotations 版本对齐（task-core 等共享模块编译注解用） -->
        <swagger-annotations.version>2.2.43</swagger-annotations.version>
        <!-- MinIO Java Client（Sprint 9 F2：savepoint 文件物理清理；Maven Central 8.5.17 为 8.x 最新稳定版） -->
        <minio.version>8.5.17</minio.version>
        <!-- MySQL Connector/J 8.x 线：realtime 提交端对齐 Flink CDC 3.6.0-2.2 依赖；业务库驱动不受影响（走 Boot BOM 9.x） -->
        <mysql-connector.version>8.0.33</mysql-connector.version>
        <!-- Apache POI（xlsx 导出统一框架：governance / data-service 共用） -->
        <poi.version>5.4.1</poi.version>
        <!-- PowerJob（与 middleware-powerjob 5.1.2 容器全局绑定，worker / job 共享） -->
        <powerjob.version>5.1.2</powerjob.version>
        <!-- 独立 Flink 2.2.1 Session 集群（realtime CDC 提交端对齐，Sprint 8 M0 已实测锁定） -->
        <flink.version>2.2.1</flink.version>
        <!-- Flink CDC 3.6.0-2.2（realtime 提交端 YAML 解析/连接器统一版本） -->
        <cdc.version>3.6.0-2.2</cdc.version>
        <!-- flink-core 2.2.1 配套 shaded guava（CDC 传递依赖带 31.1，显式锁定 33.4.0-jre-20.0） -->
        <flink-shaded-guava.version>33.4.0-jre-20.0</flink-shaded-guava.version>
        <!-- Nimbus JOSE + JWT（Sprint 14 SSO：自研 OIDC 客户端验签 id_token / 解析 JWK，决策 D8） -->
        <nimbus-jose-jwt.version>9.47</nimbus-jose-jwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud Alibaba BOM -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Sa-Token -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot4-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-reactor-spring-boot4-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-redis-template</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-aop</artifactId>
                <version>${sa-token.version}</version>
            </dependency>

            <!-- MyBatis-Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- Flyway -->
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-database-postgresql</artifactId>
                <version>${flyway.version}</version>
            </dependency>

            <!-- Commons Pool2 -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-pool2</artifactId>
                <version>${commons-pool2.version}</version>
            </dependency>

            <!-- JSqlParser（Sprint 10 F1 SQL 终端只读校验；版本对齐 mybatis-plus-jsqlparser 传递线 5.2） -->
            <dependency>
                <groupId>com.github.jsqlparser</groupId>
                <artifactId>jsqlparser</artifactId>
                <version>5.2</version>
            </dependency>

            <!-- Commons CSV（导出统一框架；esc 转义/引号由 CSVPrinter 负责） -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-csv</artifactId>
                <version>${commons-csv.version}</version>
            </dependency>

            <!-- springdoc OpenAPI（接口文档；webmvc 供业务服务，webflux-ui 供网关聚合页） -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger-annotations.version}</version>
            </dependency>

            <!-- DataNest Common -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Alert API（微服务化改造：告警服务 Feign 契约） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-alert-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest System API（微服务化改造：系统服务 Feign 契约） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-system-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Engineering API（微服务化改造：工程服务 Feign 契约） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-engineering-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Governance API（微服务化改造：治理服务 Feign 契约） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-governance-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Realtime API（Sprint 8 F2：实时 CDC 服务 Feign 契约） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-realtime-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Data Service API（Sprint 10 F1：SQL 控制台/数据服务 Feign 契约） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-data-service-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Task Core -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-task-core</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Data Service（Sprint 10：SQL 终端 / 数据 API / 实时订阅） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-data-service</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Alert Service（微服务化改造：独立告警服务） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-alert-service</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- DataNest Realtime Service（Sprint 8 F2：实时 CDC 管道服务） -->
            <dependency>
                <groupId>com.datanest</groupId>
                <artifactId>data-nest-realtime</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- MinIO Java Client（Sprint 9 F2：savepoint 文件物理清理） -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>

            <!-- Apache POI（xlsx 导出统一框架：governance / data-service） -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- PowerJob（与 middleware-powerjob 容器版本全局绑定；worker / job 共享） -->
            <dependency>
                <groupId>tech.powerjob</groupId>
                <artifactId>powerjob-worker-spring-boot-starter</artifactId>
                <version>${powerjob.version}</version>
            </dependency>

            <!-- Nimbus JOSE + JWT（Sprint 14 SSO：OIDC id_token 验签 / JWK 解析，无 Spring Security 依赖） -->
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>${nimbus-jose-jwt.version}</version>
            </dependency>

            <!-- Flink CDC 提交端依赖集（Sprint 8 M0 验证范本照抄，realtime 专用；版本统一由根 pom 管理） -->
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-cli</artifactId>
                <version>${cdc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-composer</artifactId>
                <version>${cdc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-flink2-compat</artifactId>
                <version>${cdc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-pipeline-connector-mysql</artifactId>
                <version>${cdc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-pipeline-connector-postgres</artifactId>
                <version>${cdc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-pipeline-connector-iceberg</artifactId>
                <version>${cdc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-cdc-pipeline-connector-kafka</artifactId>
                <version>${cdc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-clients</artifactId>
                <version>${flink.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-connector-base</artifactId>
                <version>${flink.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-table-common</artifactId>
                <version>${flink.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-json</artifactId>
                <version>${flink.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.flink</groupId>
                <artifactId>flink-shaded-guava</artifactId>
                <version>${flink-shaded-guava.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <configuration>
                        <layers>
                            <enabled>true</enabled>
                        </layers>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
