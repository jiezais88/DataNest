# 2026-07-24 工作日志

## DataNest Sprint 1 产品 PRD

- **输出文件**：`docs/sprint1/DataNest-Sprint1-数据源连接与元数据采集-PRD.md`
- **Sprint 1 范围**：数据源连接管理（MySQL/PostgreSQL/Doris）+ 元数据手动采集 + 连接安全
- **文档风格**：纯产品视角，无接口/表设计，重点描述页面交互和状态流转
- **交互式确认已锁定决策（v2.1）**：
    - 元数据采集范围：所有已接入数据源（MySQL/PostgreSQL/Doris）
    - 注释编辑权限：仅治理员/超管可编辑，工程师仅查看
    - Doris 数据源：保留，支持接入外部 Doris 集群
    - 菜单命名：「数据源」+「元数据采集任务」+「元数据管理」
    - 删除数据源：保留已采集元数据并标记「源已下线」
    - 任务与数据源关系：一个任务绑定一个数据源，可选该数据源下多个库/Schema
    - 采集粒度：数据源 → 库/Schema（MySQL/PG/Doris 分别对应 database/schema/database）
    - 采集模式：全量+增量（首次全量，后续仅同步变更）
    - 触发方式：手动+定时（Cron），任务异步执行
    - 删除任务：级联删除历史记录
    - 入口位置：独立一级菜单「元数据采集任务」
    - 并发控制：同一数据源下不同任务不互斥，同一任务内多个库/Schema 可并行
    - 历史记录：完整日志，详情按库/Schema 拆分
    - 结果展示：任务成功后元数据管理页自动刷新
- **关键设计**：
    - 新增「数据源」和「元数据管理」两个菜单
    - 4 个角色权限矩阵：工程师管连接，治理员补注释，分析师仅查看
    - 密码加密存储，编辑时显示脱敏占位符 +「修改密码」按钮
    - 元数据按「数据源 → 库/Schema → 表 → 字段」四级树形浏览

## 修订：数据源删除引用校验（v2.2）

- **更新文件**：`docs/sprint1/DataNest-Sprint1-数据源连接与元数据采集-PRD.md`
- **新增规则**：数据源被元数据采集任务引用时禁止删除，系统弹窗提示引用任务列表
- **产品影响**：
    - 数据源列表：删除按钮置灰，悬停显示「已被 N 个任务引用，不可删除」
    - 删除弹窗：无引用时二次确认；有引用时阻断并列出引用任务
    - 任务删除：解除引用关系后，数据源可删除的前提条件减少
- **验收标准更新**：AC-16 增加引用校验的通过标准

## 修订：切换调度执行器为 XXL-Job（v2.3）

- **更新文件**：`docs/sprint1/DataNest-Sprint1-数据源连接与元数据采集-PRD.md`
- **决策**：元数据采集任务状态只保留四个，移除「排队中」，由调度执行器承载实现细节
- **产品影响**：
    - 移除任务状态「排队中」，平台侧状态只保留「未执行 / 运行中 / 正常 / 失败」
    - 手动执行：点击后由调度执行器直接触发，状态直接变为「运行中」
    - 列表筛选、状态说明、执行提示、日志文案、验收标准 AC-7 同步移除「排队中」
    - 进度刷新通过轮询即可，不依赖队列/WebSocket 概念
- **风险更新**：R4 改为使用调度框架，支持错过触发策略

- **Sprint 1 技术文档输出**：`docs/sprint1/DataNest-Sprint1-技术文档.md`（823 行）
    - 新增 engineering-service（port 8082）+ governance-service（port 8084）两个微服务
    - engineering：数据源 CRUD、AES-256-GCM 加密、连接测试、拉取库/Schema、删除引用校验
    - governance：采集任务 CRUD、@Async 异步执行引擎、Cron 调度、全量+增量、日志存储、元数据树形浏览+内联注释编辑
    - DB 新增 7 张表（datasource_connection / collect_task / collect_history / collect_execution_log / metadata_table /
      metadata_column + 索引）
    - 共享配置新增 shared-encryption.yaml
    - 前端 3 个页面 + 菜单按角色动态显隐
    - 4 个 ADR（TaskScheduler vs Quartz / switch-case vs 抽象 / AES-256-GCM / 增量对比策略）

## Sprint 1 UI 原型产出

- 产出完整 HTML 原型：`docs/sprint1/ui/Sprint1-数据源与元数据采集.html`
- 覆盖 PRD 全部页面：数据源列表/新增编辑抽屉/删除确认、采集任务列表/创建编辑抽屉/历史记录、元数据管理树形浏览/表详情注释编辑
- 交互覆盖：测试连接、端口自动填充、Cron 联动、删除引用校验、执行详情/日志弹窗
- 复用 Sprint 0 方案 B 设计规范（#f7f8fa 底 + #4f46e5 靛蓝强调色 + Inter 字体）
- 仅产出一个 HTML 预览文件（DESIGN.md / tokens.css / tailwind.config.ts 复用 Sprint 0）

## 修订：Sprint 1 UI 设计规范独立存放（v1.1）

- 不再复用 Sprint 0 的设计规范文件，改为在 `docs/sprint1/ui/` 独立维护
- 新增 `docs/sprint1/ui/DESIGN.md`：完整继承 Sprint 0 九章节，补充 11 个 Sprint 1 组件规范
    - Drawer / Pagination / Empty State / Tree Browser / Metadata Detail Panel
    - Progress Bar / Radio Group / Multi-select Chips / Breadcrumb / Code Log / Status Variants
- 新增 `docs/sprint1/ui/tokens.css`：继承 Sprint 0 token，头部注释说明版本归属
- 新增 `docs/sprint1/ui/tailwind.config.ts`：继承 Sprint 0 配置，扩展 Sprint 1 专用尺寸（drawer 560px / tree 240px / log
  360px）和等宽字体

## Sprint 1 技术文档修订：XXL-JOB 复用 nacos-mysql

- 发现 Sprint 0 实际 `data-nest/docker-compose.yml` 已使用 `nacos-mysql`（与 Sprint 0 技术文档中的 Derby 描述不一致）
- Sprint 1 技术文档 v1.4 不再新增 `datanest-store`，改为复用 `nacos-mysql` 并新增 `datanest_scheduler` 库
- 更新：§4.1 MySQL 复用说明、§4.2 XXL-JOB JDBC URL、启动顺序、端口速查表、交付物清单、修订记录
- 待同步：Sprint 0 技术文档需与实际 docker-compose.yml 保持一致（Nacos 用 MySQL + Redis 存在）
- 手动执行接口 `/api/governance/collect-tasks/{id}/execute` 明确通过 `SchedulerService.trigger(task)` 提交到 XXL-JOB，与
  Cron 统一走调度中心
- 任务创建时即注册到 XXL-JOB：CRON 类型带 Cron 表达式，MANUAL 类型设为 `ScheduleType.NONE`；编辑/删除同步更新/注销
