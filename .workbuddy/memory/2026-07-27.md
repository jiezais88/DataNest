# 2026-07-27 工作日志

## DataNest Sprint 2 产品 PRD

- **输出文件**：`docs/sprint2/DataNest-Sprint2-批量数据同步与数据标准-PRD.md`
- **交互式确认已锁定决策**：
    - 目标端仅支持 Doris（后续 Sprint 扩至其他数据源）
    - 保存数据源后自动采集：勾选复选框触发，非默认触发
    - 失败重试：任务级别可配置（0-3 次，间隔 1-30 分钟）
    - 告警通知：PRD 只写「支持告警通知」，不指定具体渠道
- **Sprint 2 范围（5 大模块）**：
    1. **批量数据同步**：创建/编辑/删除/执行/历史/日志；源端支持 MySQL/PostgreSQL/Doris，目标端固定 Doris；全量+增量模式；手动+Cron
       触发；同步后自动更新元数据
    2. **数据预览**：数据源列表 + 元数据管理页均支持预览前 100 行
    3. **保存后自动采集**：新增数据源时勾选复选框，自动创建一次采集任务
    4. **失败重试与告警**：可配重试次数和间隔；失败后自动告警
    5. **数据标准管理**：命名规范（表/字段、前缀/后缀/正则）+ 字段类型标准 + 合规检查
- **设计原则**：遵循 Sprint 1 PRD 的纯产品视角，无接口/表设计
- **版本**：v1.1（2026-07-27 同日修订，见下方）

### v1.1 修订（2026-07-27）

- **五处产品调整**：
    1. 同步任务状态「正常」→「成功」
    2. 数据库/Schema 字段根据数据源类型动态显示：MySQL/Doris 为「选择数据库」（源库→表两级），PostgreSQL 为「选择
       Schema」（源库→Schema→表三级）
    3. 列表新增触发方式、调度状态（正常/已暂停）、下次执行时间三列；支持暂停/恢复定时调度
    4. 日志明确展示 Addax 执行日志（Reader/Writer 信息）
    5. 同步成功后元数据注册到内置 Doris 节点，明确内置 Doris 在元数据管理中默认存在，无需采集任务

## DataNest Sprint 2 技术文档

- **输出文件**：`docs/sprint2/DataNest-Sprint2-技术文档.md`
- **技术决策（杰仔确认）**：
    1. Addax 集成：Dockerfile 基于 `wgzhao/addax:6.0.11` 构建，engineering 容器内通过 `ProcessBuilder` 调 `addax.sh`
    2. 调度引擎：继续用 XXL-JOB，engineering 注册为第二个 Executor（appname: datanest-engineering-executor）
    3. 元数据注册：engineering 同步后直接写 metadata_table/column（延续 Sprint 1 的「直接查表」模式，无 Feign）
    4. 数据预览分页：MySQL/PG/Doris 均支持 LIMIT，预留 `PaginationDialect` 扩展点
    5. metadata_table 新增 `source_type` 字段（BUILTIN_DORIS / EXTERNAL），datasource_id 改为可空
- **关键 ADR**：
    - ADR-S2-001: Addax 命令行调用（非内嵌 JAR）
    - ADR-S2-002: metadata_table.source_type 区分内置/外部
    - ADR-S2-003: 同步后直接写元数据表（延续 Sprint 1 模式）
    - ADR-S2-004: 分页语法预留扩展点
    - ADR-S2-005: 重试基于 XXL-JOB + ScheduledExecutorService
- **数据库变更**（Flyway V3.0.x）：
    - metadata_table 加 source_type + datasource_id 改可空
    - 新增 sync_task / sync_history / sync_execution_log
    - 新增 naming_standard / field_type_standard
- **新增 Nacos 配置**：shared-addax.yaml（Addax 路径 + Doris Stream Load 参数）
- **v1.1 修订（杰仔反馈）**：Dockerfile 多阶段构建（Addax→JDK21）、去掉 Python、Doris 单独部署不在 docker-compose、healthcheck
  start_period 20s、XXL-JOB 明确引用 Sprint 1 代码

## Sprint 2 UI 原型与设计规范产出

- 产出 4 个 UI 文件至 `docs/sprint2/ui/`：
    - `Sprint2-批量数据同步与数据标准.html`：完整交互原型（71KB）
    - `DESIGN.md`：继承 Sprint 1，新增 5 个组件规范（字段映射表、Cron 预设、合规检查分组、数据预览抽屉等）
    - `tokens.css` / `tailwind.config.ts`：继承 Sprint 1 设计 token，底座未变
- **原型覆盖的交互视图**：
    - **批量数据同步**：列表（3 任务涵盖 4 种状态 + 重试中/停用调度）、创建/编辑抽屉（640px，含字段映射表+12 Cron
      预设+容错配置）、历史记录页、详情/日志弹窗（Addax 日志）、删除确认弹窗
    - **数据标准**：命名规范列表 + 字段类型标准列表（Tab
      切换）、新建命名规范弹窗、新建类型标准弹窗、合规检查配置弹窗、合规检查结果页（命名不合规 + 类型不合规分组）
    - **数据预览**：从数据源进入的左侧库/Schema 树 + 右侧 100 行预览表格
- 顶部主 Tab 切换「批量数据同步 | 数据标准」，每个 Tab 有独立侧边栏和面包屑联动
- JS 交互覆盖：Tab 切换/面包屑返回/抽屉弹窗/字段映射增删/调度启停/Cron 预设/Copy 日志/Toast 提示
